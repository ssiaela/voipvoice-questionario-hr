import React,{useEffect,useMemo,useState} from 'react'
import {Routes,Route,Navigate,NavLink,useNavigate,useParams,useLocation} from 'react-router-dom'
import {supabase} from './supabase'
import {LayoutDashboard,Users,ClipboardList,Settings,LogOut,BarChart3,Copy,Plus,ChevronLeft,ChevronRight,Save,Clock3,CircleCheckBig,TriangleAlert,FileText,UserRound,ArrowRight,ShieldCheck,Download,FileDown,KeyRound,Moon,Sun,Monitor,LockKeyhole} from 'lucide-react'
import {Radar,RadarChart,PolarGrid,PolarAngleAxis,ResponsiveContainer} from 'recharts'
import * as XLSX from 'xlsx'
import jsPDF from 'jspdf'
import autoTable from 'jspdf-autotable'

const NOTE='I punteggi descrivono tendenze emerse dalle risposte e servono a orientare il colloquio. Non costituiscono una valutazione psicologica o una misurazione psicometrica validata.'
const comps=['Accountability e affidabilità','Problem solving e analisi','Collaborazione e comunicazione','Adattabilità','Orientamento al risultato','Autonomia e iniziativa']

const COMPETENCY_META={
  'Accountability e affidabilità':{color:'#2563EB',lines:['Accountability','e affidabilità']},
  'Problem solving e analisi':{color:'#7C3AED',lines:['Problem solving','e analisi']},
  'Collaborazione e comunicazione':{color:'#10B981',lines:['Collaborazione','e comunicazione']},
  'Adattabilità':{color:'#F59E0B',lines:['Adattabilità']},
  'Orientamento al risultato':{color:'#EF4444',lines:['Orientamento','al risultato']},
  'Autonomia e iniziativa':{color:'#06B6D4',lines:['Autonomia','e iniziativa']}
}
function competencyMeta(name){return COMPETENCY_META[name]||{color:'#0874BD',lines:[name]}}
function RadarAxisTick({payload,x,y,textAnchor}){
  const meta=competencyMeta(payload?.value)
  const lines=meta.lines||[payload?.value||'']
  const startDy=lines.length>1?-6:4
  return <g transform={`translate(${x},${y})`}><text textAnchor={textAnchor||'middle'} fill={meta.color} fontSize="12" fontWeight="700">{lines.map((line,i)=><tspan key={line} x="0" dy={i===0?startDy:14}>{line}</tspan>)}</text></g>
}

const THEME_KEY='vv-hr-theme'
function resolvedTheme(theme){if(theme==='system')return window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light';return theme}
function applyTheme(theme){document.documentElement.dataset.theme=resolvedTheme(theme)}
function setThemePreference(theme){localStorage.setItem(THEME_KEY,theme);applyTheme(theme);window.dispatchEvent(new Event('vv-theme-change'))}
function safeFileName(value){return String(value||'report').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9-_]+/g,'-').replace(/^-+|-+$/g,'').toLowerCase()||'report'}
function resultBaseName(r){return safeFileName(`${r?.candidates?.first_name||'candidato'}-${r?.candidates?.last_name||''}-${r?.completed_at?.slice?.(0,10)||''}`)}
function summaryRow(r){const row={Candidato:`${r.candidates?.first_name||''} ${r.candidates?.last_name||''}`.trim(),Posizione:r.candidates?.position||'',Completato:fmt(r.completed_at),Versione:`v${r.questionnaire_versions?.version_number||''}`,'Punteggio scenari':Number(r.scenario_score||0)};for(const[name,v]of Object.entries(r.competencies||{}))row[name]=Number(v.score||0);return row}
function answerRows(r){return (r.answers_snapshot||[]).map(a=>({Candidato:`${r.candidates?.first_name||''} ${r.candidates?.last_name||''}`.trim(),Posizione:r.candidates?.position||'',Domanda:a.number,Testo:a.question,Risposta:`${a.answer}. ${a.answer_label}`,Punteggio:Number(a.normalized_score||0)}))}
function exportResultExcel(r){const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet([summaryRow(r)]),'Risultato');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(answerRows(r)),'Risposte');XLSX.writeFile(wb,`${resultBaseName(r)}.xlsx`)}
function exportResultsExcel(list){if(!list.length)return;const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(list.map(summaryRow)),'Risultati');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(list.flatMap(answerRows)),'Risposte');XLSX.writeFile(wb,`risultati-questionario-${new Date().toISOString().slice(0,10)}.xlsx`)}
function exportResultPdf(r){const doc=new jsPDF({unit:'mm',format:'a4'});const name=`${r.candidates?.first_name||''} ${r.candidates?.last_name||''}`.trim();doc.setFont('helvetica','bold');doc.setFontSize(18);doc.text('VoipVoice - Risultato questionario',14,18);doc.setFont('helvetica','normal');doc.setFontSize(10);doc.text(`Candidato: ${name}`,14,27);doc.text(`Posizione: ${r.candidates?.position||'-'}`,14,33);doc.text(`Completato: ${fmt(r.completed_at)}   Versione: v${r.questionnaire_versions?.version_number||'-'}`,14,39);autoTable(doc,{startY:47,head:[['Competenza','Punteggio','Fascia','Indicazione colloquio']],body:Object.entries(r.competencies||{}).map(([n,v])=>[n,Number(v.score||0).toFixed(1),v.label||'',v.interview_guidance||'']),styles:{fontSize:8,cellPadding:2.2},headStyles:{fillColor:[8,116,189]}});let y=(doc.lastAutoTable?.finalY||47)+8;doc.setFont('helvetica','bold');doc.setFontSize(10);doc.text(`Punteggio scenari: ${Number(r.scenario_score||0).toFixed(1)}/100`,14,y);doc.setFont('helvetica','normal');doc.setFontSize(8);const note=doc.splitTextToSize(NOTE,180);doc.text(note,14,y+6);autoTable(doc,{startY:y+15,head:[['#','Domanda','Risposta']],body:(r.answers_snapshot||[]).map(a=>[a.number,a.question,`${a.answer}. ${a.answer_label}`]),styles:{fontSize:7,cellPadding:1.8,overflow:'linebreak'},columnStyles:{0:{cellWidth:10},1:{cellWidth:112},2:{cellWidth:56}},headStyles:{fillColor:[8,116,189]}});doc.save(`${resultBaseName(r)}.pdf`)}


function App(){useEffect(()=>{const sync=()=>applyTheme(localStorage.getItem(THEME_KEY)||'light');sync();const media=window.matchMedia('(prefers-color-scheme: dark)');media.addEventListener?.('change',sync);window.addEventListener('vv-theme-change',sync);return()=>{media.removeEventListener?.('change',sync);window.removeEventListener('vv-theme-change',sync)}},[]);return <Routes><Route path="/login" element={<Login/>}/><Route path="/q/:token" element={<CandidateTest/>}/><Route path="/hr/*" element={<HrGuard><HrLayout/></HrGuard>}/><Route path="*" element={<Navigate to="/hr" replace/>}/></Routes>}

function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const nav=useNavigate();async function submit(e){e.preventDefault();setError('');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);else nav('/hr')}return <div className="auth-page"><div className="auth-card"><Brand/><h1>Area HR</h1><p>Accesso riservato agli utenti autorizzati.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="btn primary">Accedi</button></form></div></div>}
function Brand(){return <div className="brand"><img src="/logo-voipvoice.svg" alt="VoipVoice" className="brand-logo"/><span>People & Culture</span></div>}
function HrGuard({children}){const[state,setState]=useState('loading');useEffect(()=>{(async()=>{const{data:{session}}=await supabase.auth.getSession();if(!session)return setState('no');const{data}=await supabase.from('hr_users').select('id').eq('id',session.user.id).maybeSingle();setState(data?'ok':'no')})()},[]);if(state==='loading')return <Loading/>;if(state==='no')return <Navigate to="/login" replace/>;return children}
function HrLayout(){const nav=useNavigate();const[email,setEmail]=useState('');const items=[['/hr',LayoutDashboard,'Dashboard'],['/hr/candidature',Users,'Candidature'],['/hr/risultati',BarChart3,'Risultati'],['/hr/questionario',ClipboardList,'Questionario'],['/hr/impostazioni',Settings,'Impostazioni']];useEffect(()=>{supabase.auth.getUser().then(({data})=>setEmail(data.user?.email||''))},[]);const initials=(email||'HR').split('@')[0].split(/[._-]/).map(x=>x[0]).join('').slice(0,2).toUpperCase();return <div className="shell"><aside><Brand/><nav>{items.map(([to,I,label])=><NavLink key={to} end={to==='/hr'} to={to}><I size={18}/>{label}</NavLink>)}</nav><div className="sidebar-foot"><div className="sidebar-version"><ShieldCheck size={15}/><span>Area HR protetta</span></div><button className="logout" onClick={async()=>{await supabase.auth.signOut();nav('/login')}}><LogOut size={17}/>Esci</button></div></aside><div className="workspace"><div className="topbar"><div><span className="topbar-kicker">VoipVoice</span><strong>People & Culture</strong></div><div className="topbar-user"><span className="avatar">{initials}</span><div><strong>{email||'Utente HR'}</strong><small>HR autorizzato</small></div></div></div><main><Routes><Route index element={<Dashboard/>}/><Route path="candidature" element={<Candidates/>}/><Route path="risultati" element={<Results/>}/><Route path="questionario" element={<QuestionnaireEditor/>}/><Route path="impostazioni" element={<SettingsPage/>}/></Routes></main></div></div>}
function Page({title,subtitle,children,action}){return <><header className="page-head"><div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div>{action}</header>{children}</>}
function Loading(){return <div className="center">Caricamento…</div>}

function Dashboard(){
  const[data,setData]=useState([]),[results,setResults]=useState([]),[version,setVersion]=useState(null),[last30,setLast30]=useState(0)
  const nav=useNavigate()
  useEffect(()=>{load()},[])
  async function load(){
    const from30=new Date(Date.now()-30*24*60*60*1000).toISOString()
    const[{data:c},{data:r},{data:v},{count}]=await Promise.all([
      supabase.from('candidates').select('*,invitations(expires_at)').order('created_at',{ascending:false}),
      supabase.from('candidate_results').select('completed_at,competencies,candidates(first_name,last_name,position)').order('completed_at',{ascending:false}).limit(5),
      supabase.from('questionnaire_versions').select('version_number,published_at').eq('status','published').order('version_number',{ascending:false}).limit(1).maybeSingle(),
      supabase.from('candidate_results').select('id',{count:'exact',head:true}).gte('completed_at',from30)
    ])
    setData(c||[]);setResults(r||[]);setVersion(v||null);setLast30(count||0)
  }
  const now=new Date()
  const isExpired=c=>c.status!=='completato'&&c.invitations?.[0]?.expires_at&&new Date(c.invitations[0].expires_at)<now
  const expired=data.filter(isExpired).length
  const pending=data.filter(c=>c.status!=='completato'&&!isExpired(c)).length
  const completed=data.filter(c=>c.status==='completato').length
  const cards=[
    {label:'Candidature totali',value:data.length,icon:Users,tone:'blue'},
    {label:'Da completare',value:pending,icon:Clock3,tone:'amber'},
    {label:'Completate',value:completed,icon:CircleCheckBig,tone:'green'},
    {label:'Scadute',value:expired,icon:TriangleAlert,tone:'red'}
  ]
  const recentCandidates=data.slice(0,6),latest=data[0]
  function status(c){if(isExpired(c))return 'Scaduto';return {da_compilare:'Da compilare',iniziato:'Iniziato',completato:'Completato',scaduto:'Scaduto'}[c.status]||c.status}
  return <Page title="Dashboard HR" subtitle="Panoramica operativa del questionario attitudinale" action={<button className="btn primary" onClick={()=>nav('/hr/candidature',{state:{openNew:true}})}><Plus size={17}/>Nuova candidatura</button>}>
    <div className="kpis dashboard-kpis">{cards.map(({label,value,icon:I,tone})=><div className={'kpi kpi-'+tone} key={label}><div className="kpi-icon"><I size={22}/></div><div><span>{label}</span><strong>{value}</strong></div></div>)}</div>
    <section className="panel dashboard-panel dashboard-candidates">
      <div className="panel-title-row"><div><h2>Ultime candidature</h2><p>Le candidature create più recentemente</p></div><button className="panel-link" onClick={()=>nav('/hr/candidature')}>Vedi tutte <ArrowRight size={15}/></button></div>
      {recentCandidates.length?<div className="table-wrap"><table className="dashboard-table"><thead><tr><th>Candidato</th><th>Posizione</th><th>Stato</th><th>Scadenza</th></tr></thead><tbody>{recentCandidates.map(c=><tr key={c.id}><td><div className="person-cell"><span className="mini-avatar">{(c.first_name?.[0]||'')+(c.last_name?.[0]||'')}</span><strong>{c.first_name} {c.last_name}</strong></div></td><td>{c.position}</td><td><span className={'badge '+status(c).toLowerCase().replaceAll(' ','-')}>{status(c)}</span></td><td>{c.invitations?.[0]?.expires_at?fmtDate(c.invitations[0].expires_at):'—'}</td></tr>)}</tbody></table></div>:<Empty text="Nessuna candidatura."/>}
    </section>
    <div className="dashboard-lower">
      <section className="panel dashboard-panel results-panel">
        <div className="panel-title-row"><div><h2>Ultimi risultati</h2><p>Una lettura rapida degli ultimi test completati</p></div><button className="panel-link" onClick={()=>nav('/hr/risultati')}>Vedi tutti <ArrowRight size={15}/></button></div>
        {results.length?<div className="result-card-list">{results.map((r,i)=>{const top=Object.entries(r.competencies||{}).sort((a,b)=>Number(b[1].score)-Number(a[1].score)).slice(0,3);return <article className="result-card-row" key={i}><div className="result-person"><span className="mini-avatar">{(r.candidates?.first_name?.[0]||'')+(r.candidates?.last_name?.[0]||'')}</span><div><strong>{r.candidates?.first_name} {r.candidates?.last_name}</strong><small>{r.candidates?.position}</small><small>{fmtDate(r.completed_at)}</small></div></div><div className="result-highlights">{top.map(([name,v])=>{const meta=competencyMeta(name);return <div className="result-highlight" key={name}><div><span title={name}><i className="competency-dot" style={{background:meta.color}}/>{name}</span><b style={{color:meta.color}}>{Number(v.score).toFixed(0)}</b></div><div className="mini-score-track"><i style={{width:`${Math.max(0,Math.min(100,Number(v.score)||0))}%`,background:meta.color}}/></div></div>})}</div></article>})}</div>:<Empty text="Nessun questionario completato."/>}
      </section>
      <aside className="dashboard-side">
        <section className="panel operational-panel"><div className="panel-title-row compact"><div><h2>Riepilogo operativo</h2><p>Informazioni utili a colpo d'occhio</p></div></div><div className="operational-stats"><div className="operational-item"><div className="summary-icon"><BarChart3 size={20}/></div><div><span>Completati ultimi 30 giorni</span><strong>{last30}</strong></div></div><div className="operational-item"><div className="summary-icon"><UserRound size={20}/></div><div><span>Ultima candidatura</span><strong>{latest?`${latest.first_name} ${latest.last_name}`:'—'}</strong><small>{latest?.position||'Nessuna candidatura'}</small></div></div><div className="operational-item"><div className="summary-icon"><FileText size={20}/></div><div><span>Versione attiva</span><strong>{version?`v${version.version_number}`:'—'}</strong><small>{version?.published_at?`Pubblicata ${fmtDate(version.published_at)}`:'Nessuna versione pubblicata'}</small></div></div></div></section>
        <div className="method-note method-card"><span className="method-icon">i</span><div><strong>Nota metodologica</strong><p>{NOTE}</p></div></div>
      </aside>
    </div>
  </Page>
}

function Candidates(){const[list,setList]=useState([]),[open,setOpen]=useState(false),[link,setLink]=useState(''),[form,setForm]=useState({first_name:'',last_name:'',position:'',expires_at:''});const routeLocation=useLocation(),nav=useNavigate();useEffect(()=>{load();if(routeLocation.state?.openNew){setOpen(true);nav(routeLocation.pathname,{replace:true,state:null})}},[]);async function load(){const{data}=await supabase.from('candidates').select('*,invitations(expires_at),questionnaire_results:candidate_results(id)').order('created_at',{ascending:false});setList(data||[])}async function create(e){e.preventDefault();const{data:{user}}=await supabase.auth.getUser();const{data:c,error}=await supabase.from('candidates').insert({...form,expires_at:undefined,created_by:user.id}).select().single();if(error)return alert(error.message);const exp=form.expires_at?new Date(form.expires_at).toISOString():null;const{data:token,error:re}=await supabase.rpc('create_candidate_invitation',{p_candidate_id:c.id,p_expires_at:exp});if(re)return alert(re.message);const base=import.meta.env.VITE_PUBLIC_SITE_URL||location.origin;setLink(`${base}/q/${token}`);setForm({first_name:'',last_name:'',position:'',expires_at:''});setOpen(false);load()}function status(c){const exp=c.invitations?.[0]?.expires_at;if(c.status!=='completato'&&exp&&new Date(exp)<new Date())return 'Scaduto';return {da_compilare:'Da compilare',iniziato:'Iniziato',completato:'Completato',scaduto:'Scaduto'}[c.status]}return <Page title="Candidature" subtitle="Genera e gestisci i link personali" action={<button className="btn primary" onClick={()=>setOpen(true)}><Plus size={17}/>Nuova candidatura</button>}><section className="panel table-wrap"><table><thead><tr><th>Candidato</th><th>Posizione</th><th>Stato</th><th>Creata</th></tr></thead><tbody>{list.map(c=><tr key={c.id}><td><strong>{c.first_name} {c.last_name}</strong></td><td>{c.position}</td><td><span className={'badge '+status(c).toLowerCase().replaceAll(' ','-')}>{status(c)}</span></td><td>{fmt(c.created_at)}</td></tr>)}</tbody></table>{!list.length&&<Empty text="Nessuna candidatura."/>}</section>{open&&<Modal onClose={()=>setOpen(false)} title="Nuova candidatura"><form onSubmit={create} className="form-grid"><label>Nome<input required value={form.first_name} onChange={e=>setForm({...form,first_name:e.target.value})}/></label><label>Cognome<input required value={form.last_name} onChange={e=>setForm({...form,last_name:e.target.value})}/></label><label className="full">Posizione<input required value={form.position} onChange={e=>setForm({...form,position:e.target.value})}/></label><label className="full">Scadenza link<input type="datetime-local" value={form.expires_at} onChange={e=>setForm({...form,expires_at:e.target.value})}/></label><button className="btn primary full">Genera link</button></form></Modal>}{link&&<Modal onClose={()=>setLink('')} title="Link candidatura"><p className="muted">Il token non viene salvato in chiaro nel database. Copia ora il link.</p><div className="copybox"><input readOnly value={link}/><button className="btn" onClick={()=>navigator.clipboard.writeText(link)}><Copy size={16}/>Copia</button></div></Modal>}</Page>}

function Results(){
  const[list,setList]=useState([]),[selected,setSelected]=useState(null)
  useEffect(()=>{(async()=>{const{data}=await supabase.from('candidate_results').select('*,candidates(first_name,last_name,position),questionnaire_versions(version_number)').order('completed_at',{ascending:false});setList(data||[])})()},[])
  return <Page title="Risultati" subtitle="Esiti riservati all’area HR" action={<div className="page-actions"><button className="btn" disabled={!list.length} onClick={()=>exportResultsExcel(list)}><Download size={16}/>Esporta Excel</button></div>}>
    <section className="panel table-wrap"><table><thead><tr><th>Candidato</th><th>Posizione</th><th>Completato</th><th>Versione</th><th></th></tr></thead><tbody>{list.map(r=><tr key={r.id}><td><strong>{r.candidates.first_name} {r.candidates.last_name}</strong></td><td>{r.candidates.position}</td><td>{fmt(r.completed_at)}</td><td>v{r.questionnaire_versions.version_number}</td><td><div className="row-actions"><button className="linkbtn" onClick={()=>setSelected(r)}>Apri</button><button className="icon-action" title="Scarica PDF" onClick={()=>exportResultPdf(r)}><FileDown size={16}/></button></div></td></tr>)}</tbody></table>{!list.length&&<Empty text="Nessun risultato disponibile."/>}</section>
    {selected&&<ResultModal r={selected} onClose={()=>setSelected(null)}/>} 
  </Page>
}
function ResultModal({r,onClose}){
  const competencyEntries=comps.map(name=>[name,r.competencies?.[name]]).filter(([,v])=>v)
  const radar=comps.map(name=>({name,score:Number(r.competencies?.[name]?.score||0),color:competencyMeta(name).color}))
  return <Modal wide onClose={onClose} title={`${r.candidates.first_name} ${r.candidates.last_name}`}>
    <div className="result-toolbar"><div><span>Esporta il test completo</span><small>Include punteggi, competenze e 40 risposte.</small></div><div><button className="btn" onClick={()=>exportResultExcel(r)}><Download size={16}/>Excel</button><button className="btn primary" onClick={()=>exportResultPdf(r)}><FileDown size={16}/>PDF</button></div></div>
    <div className="result-meta"><span>{r.candidates.position}</span><span>v{r.questionnaire_versions.version_number}</span><span>{fmt(r.completed_at)}</span></div>
    <div className="result-grid">
      <div className="competency-results"><h3>Competenze</h3>{competencyEntries.map(([name,v])=>{const meta=competencyMeta(name);const score=Math.max(0,Math.min(100,Number(v.score)||0));return <div className="score" key={name}><div className="score-head"><div className="score-name"><i className="competency-dot" style={{background:meta.color}}/><strong>{name}</strong></div><b className="score-badge" style={{color:meta.color,borderColor:`${meta.color}55`,background:`${meta.color}12`}}>{score.toFixed(1)}</b></div><div className="bar"><i style={{width:`${score}%`,background:meta.color}}/></div><small>{v.label} · {v.interview_guidance}</small><p>{v.suggestion}</p></div>})}<div className="scenario"><span>Punteggio scenari</span><strong>{Number(r.scenario_score).toFixed(1)}/100</strong></div></div>
      <div className="radar-panel"><div className="radar-heading"><h3>Profilo competenze</h3><p>Confronto visivo dei sei punteggi su scala 0–100.</p></div><div className="radar-chart-wrap"><ResponsiveContainer width="100%" height={430}><RadarChart data={radar} outerRadius="64%" margin={{top:52,right:112,bottom:52,left:112}}><PolarGrid stroke="var(--chart-grid)"/><PolarAngleAxis dataKey="name" tick={<RadarAxisTick/>} tickLine={false}/><Radar dataKey="score" stroke="var(--chart-stroke)" fill="var(--chart-fill)" fillOpacity={0.18} strokeWidth={2}/></RadarChart></ResponsiveContainer></div><div className="radar-legend">{radar.map(item=><div key={item.name}><i className="competency-dot" style={{background:item.color}}/><span>{item.name}</span><strong style={{color:item.color}}>{item.score.toFixed(1)}</strong></div>)}</div></div>
    </div>
    <h3>40 risposte</h3><div className="answers">{(r.answers_snapshot||[]).map(a=><div key={a.number}><b>{a.number}.</b><span>{a.question}</span><strong>{a.answer}. {a.answer_label}</strong></div>)}</div>
  </Modal>
}

function CandidateTest(){const{token}=useParams();const[data,setData]=useState(null),[err,setErr]=useState(''),[idx,setIdx]=useState(0),[answers,setAnswers]=useState({}),[done,setDone]=useState(false),[sending,setSending]=useState(false);useEffect(()=>{(async()=>{const{data,error}=await supabase.rpc('candidate_questionnaire',{p_token:token});if(error)setErr(error.message);else{setData(data);await supabase.rpc('candidate_start',{p_token:token})}})()},[token]);if(done)return <div className="candidate-page"><div className="candidate-card done"><Brand/><div className="check">✓</div><h1>Questionario completato e inviato.</h1></div></div>;if(err)return <div className="candidate-page"><div className="candidate-card"><Brand/><h1>Link non disponibile</h1><p>{err}</p></div></div>;if(!data)return <Loading/>;const q=data.questions[idx],progress=Math.round(((idx+1)/data.questions.length)*100),all=Object.keys(answers).length===40;async function submit(){if(!all)return;setSending(true);const{error}=await supabase.rpc('candidate_submit',{p_token:token,p_answers:answers});setSending(false);if(error)setErr(error.message);else setDone(true)}return <div className="candidate-page"><div className="candidate-card"><div className="candidate-top"><Brand/><div><strong>{data.candidate.first_name} {data.candidate.last_name}</strong><span>{data.candidate.position}</span></div></div><div className="progress"><i style={{width:`${progress}%`}}/></div><div className="qmeta">Domanda {idx+1} di 40 <span>{progress}%</span></div><h1 className="question">{q.text}</h1><div className="options">{q.options.map(o=><button key={o.value} className={answers[q.number]===o.value?'selected':''} onClick={()=>setAnswers({...answers,[q.number]:o.value})}><b>{o.value}</b><span>{o.label}</span></button>)}</div><div className="candidate-actions"><button className="btn" disabled={idx===0} onClick={()=>setIdx(idx-1)}><ChevronLeft size={17}/>Indietro</button>{idx<39?<button className="btn primary" disabled={!answers[q.number]} onClick={()=>setIdx(idx+1)}>Avanti<ChevronRight size={17}/></button>:<button className="btn primary" disabled={!all||sending} onClick={submit}>{sending?'Invio…':'Invia questionario'}</button>}</div></div></div>}

function QuestionnaireEditor(){const[payload,setPayload]=useState(null),[saving,setSaving]=useState(false);useEffect(()=>{load()},[]);function move(i,dir){const qs=[...payload.questions],j=i+dir;if(j<0||j>=qs.length)return;[qs[i],qs[j]]=[qs[j],qs[i]];setPayload({...payload,questions:qs})}async function load(){const{data,error}=await supabase.rpc('hr_questionnaire_json',{p_version_id:null});if(error)alert(error.message);else setPayload(data)}function setQ(i,patch){const qs=[...payload.questions];qs[i]={...qs[i],...patch};setPayload({...payload,questions:qs})}async function save(){if(!confirm('Creare e pubblicare una nuova versione del questionario?'))return;setSaving(true);const{data,error}=await supabase.rpc('hr_save_questionnaire_version',{p_payload:payload,p_publish:true});setSaving(false);if(error)alert(error.message);else{alert(`Versione ${data} pubblicata.`);load()}}if(!payload)return <Loading/>;return <Page title="Questionario" subtitle="Ogni salvataggio pubblica una nuova versione" action={<button className="btn primary" onClick={save} disabled={saving}><Save size={17}/>{saving?'Salvataggio…':'Salva nuova versione'}</button>}><div className="editor">{payload.questions.map((q,i)=><details className="panel qedit" key={q.number}><summary><b>{q.number}</b><span>{q.text}</span><em>{q.type==='scenario'?'Scenario':q.reverse?'Likert · inversa':'Likert'}</em></summary><div className="reorder"><button className="btn" onClick={()=>move(i,-1)} disabled={i===0}>↑ Sposta su</button><button className="btn" onClick={()=>move(i,1)} disabled={i===payload.questions.length-1}>↓ Sposta giù</button></div><label>Testo<textarea value={q.text} onChange={e=>setQ(i,{text:e.target.value})}/></label><div className="form-grid"><label>Competenza primaria<select value={q.primary||''} onChange={e=>setQ(i,{primary:e.target.value})}>{comps.map(c=><option key={c}>{c}</option>)}</select></label><label>Competenza secondaria<select value={q.secondary||''} onChange={e=>setQ(i,{secondary:e.target.value||null})}><option value="">Nessuna</option>{comps.map(c=><option key={c}>{c}</option>)}</select></label>{q.type==='likert'&&<label className="checkline"><input type="checkbox" checked={q.reverse} onChange={e=>setQ(i,{reverse:e.target.checked})}/>Item inverso</label>}</div><h4>Risposte e punteggi</h4>{q.options.map((o,j)=><div className="option-edit" key={o.value}><b>{o.value}</b><input value={o.label} onChange={e=>{const opts=[...q.options];opts[j]={...o,label:e.target.value};setQ(i,{options:opts})}}/><input type="number" min="1" max="5" value={o.score} onChange={e=>{const opts=[...q.options];opts[j]={...o,score:Number(e.target.value)};setQ(i,{options:opts})}}/></div>)}</details>)}<section className="panel"><h2>Fasce di valutazione</h2>{payload.ranges.map((r,i)=><div className="range-edit" key={i}><input type="number" value={r.min} onChange={e=>{const x=[...payload.ranges];x[i]={...r,min:Number(e.target.value)};setPayload({...payload,ranges:x})}}/><input type="number" value={r.max} onChange={e=>{const x=[...payload.ranges];x[i]={...r,max:Number(e.target.value)};setPayload({...payload,ranges:x})}}/><input value={r.label} onChange={e=>{const x=[...payload.ranges];x[i]={...r,label:e.target.value};setPayload({...payload,ranges:x})}}/><input value={r.guidance} onChange={e=>{const x=[...payload.ranges];x[i]={...r,guidance:e.target.value};setPayload({...payload,ranges:x})}}/></div>)}</section><section className="panel"><h2>Suggerimenti HR</h2>{comps.map(c=><label key={c}>{c}<textarea value={payload.guidance[c]||''} onChange={e=>setPayload({...payload,guidance:{...payload.guidance,[c]:e.target.value}})}/></label>)}</section></div></Page>}
function SettingsPage(){
  const[email,setEmail]=useState(''),[theme,setTheme]=useState(()=>localStorage.getItem(THEME_KEY)||'light'),[currentPassword,setCurrentPassword]=useState(''),[newPassword,setNewPassword]=useState(''),[confirmPassword,setConfirmPassword]=useState(''),[message,setMessage]=useState(null),[saving,setSaving]=useState(false)
  useEffect(()=>{supabase.auth.getUser().then(({data})=>setEmail(data.user?.email||''))},[])
  function changeTheme(value){setTheme(value);setThemePreference(value)}
  async function changePassword(e){
    e.preventDefault();setMessage(null)
    if(newPassword.length<8)return setMessage({type:'error',text:'La nuova password deve contenere almeno 8 caratteri.'})
    if(newPassword!==confirmPassword)return setMessage({type:'error',text:'Le nuove password non coincidono.'})
    setSaving(true)
    const{error:loginError}=await supabase.auth.signInWithPassword({email,password:currentPassword})
    if(loginError){setSaving(false);return setMessage({type:'error',text:'La password attuale non è corretta.'})}
    const{error}=await supabase.auth.updateUser({password:newPassword})
    setSaving(false)
    if(error)return setMessage({type:'error',text:error.message})
    setCurrentPassword('');setNewPassword('');setConfirmPassword('');setMessage({type:'success',text:'Password aggiornata correttamente.'})
  }
  async function signOutEverywhere(){if(!confirm('Disconnettere questo account HR da tutti i dispositivi?'))return;await supabase.auth.signOut({scope:'global'});window.location.href='/login'}
  const themes=[{value:'light',label:'Chiaro',icon:Sun},{value:'dark',label:'Scuro',icon:Moon},{value:'system',label:'Sistema',icon:Monitor}]
  return <Page title="Impostazioni" subtitle="Account, sicurezza e preferenze dell’area HR">
    <div className="settings-grid">
      <section className="panel settings-card settings-wide"><div className="settings-card-head"><div className="settings-card-icon"><KeyRound size={20}/></div><div><h2>Password HR</h2><p>Aggiorna la password dell’account autenticato.</p></div></div><div className="account-email"><span>Account</span><strong>{email||'—'}</strong></div><form className="password-form" onSubmit={changePassword}><label>Password attuale<input type="password" autoComplete="current-password" required value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)}/></label><div className="form-grid"><label>Nuova password<input type="password" minLength="8" autoComplete="new-password" required value={newPassword} onChange={e=>setNewPassword(e.target.value)}/></label><label>Conferma nuova password<input type="password" minLength="8" autoComplete="new-password" required value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)}/></label></div>{message&&<div className={message.type==='success'?'success-message':'error'}>{message.text}</div>}<div><button className="btn primary" disabled={saving}>{saving?'Aggiornamento…':'Aggiorna password'}</button></div></form></section>
      <section className="panel settings-card"><div className="settings-card-head"><div className="settings-card-icon"><Moon size={20}/></div><div><h2>Aspetto</h2><p>Scegli il tema dell’interfaccia.</p></div></div><div className="theme-options">{themes.map(({value,label,icon:I})=><button key={value} className={'theme-option '+(theme===value?'active':'')} onClick={()=>changeTheme(value)}><I size={19}/><span>{label}</span>{theme===value&&<CircleCheckBig size={16}/>}</button>)}</div></section>
      <section className="panel settings-card"><div className="settings-card-head"><div className="settings-card-icon"><LockKeyhole size={20}/></div><div><h2>Sessioni</h2><p>Revoca le sessioni aperte su altri dispositivi.</p></div></div><p className="settings-help">Dopo questa operazione sarà necessario effettuare nuovamente l’accesso con la password HR.</p><button className="btn danger" onClick={signOutEverywhere}>Esci da tutti i dispositivi</button></section>
      <section className="panel settings-card settings-wide"><div className="settings-card-head"><div className="settings-card-icon"><ShieldCheck size={20}/></div><div><h2>Sicurezza applicativa</h2><p>Configurazione attualmente in uso.</p></div></div><p>Accesso HR tramite Supabase Auth e autorizzazione tramite tabella <code>hr_users</code>. Il candidato utilizza esclusivamente RPC dedicate con token monouso; scoring e risultati non sono esposti alle query anonime.</p></section>
      <section className="panel settings-card settings-wide"><div className="settings-card-head"><div className="settings-card-icon"><FileText size={20}/></div><div><h2>Nota metodologica</h2><p>Indicazione da mantenere nell’uso dei risultati.</p></div></div><p>{NOTE}</p></section>
    </div>
  </Page>
}
function Modal({title,onClose,children,wide=false}){return <div className="overlay" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className={'modal '+(wide?'wide':'')}><div className="modal-head"><h2>{title}</h2><button onClick={onClose}>×</button></div>{children}</div></div>}
function Empty({text}){return <div className="empty">{text}</div>}
function fmtDate(d){return d?new Intl.DateTimeFormat('it-IT',{dateStyle:'medium'}).format(new Date(d)):'—'}
function fmt(d){return d?new Intl.DateTimeFormat('it-IT',{dateStyle:'medium',timeStyle:'short'}).format(new Date(d)):'—'}
export default App
